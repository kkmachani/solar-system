apiVersion: v1
kind: Service
metadata:
  labels:
    app: solar-system
  name: solarsystem
  namespace: _{_NAMESPACE_}_
spec:
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: solar-system 
  type: NodePort